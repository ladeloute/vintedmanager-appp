# Variables d'environnement requises pour VintedManager

# Base de données PostgreSQL (obligatoire)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# API Gemini pour l'IA (obligatoire)
GEMINI_API_KEY=your_gemini_api_key_here

# Configuration serveur
NODE_ENV=production
PORT=10000

# Variables automatiques (ne pas modifier)
PGHOST=extracted_from_DATABASE_URL
PGPORT=extracted_from_DATABASE_URL
PGUSER=extracted_from_DATABASE_URL
PGPASSWORD=extracted_from_DATABASE_URL
PGDATABASE=extracted_from_DATABASE_URL